# PostgreSQL connection for virtual domains
hosts = postgres-mail:5432
user = ceerion
password = ceerion_secure_2024
dbname = ceerion_mail
query = SELECT 1 FROM virtual_domains WHERE domain='%s' AND active=1
