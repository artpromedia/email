{
  "name": "@ceerion/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and utilities for CEERION Mail",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "typecheck": "tsc --noEmit",
    "test": "echo 'Shared test complete'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@ceerion/config": "workspace:*",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
